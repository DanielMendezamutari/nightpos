# Build de producción — hosting cPanel (estabilización rollback PWA)
# Frontend: https://nightpos.ribersoft.com
# API temporal legacy (sin rewrite /api/ en raíz): /backend/public/api/v1
# Volver a /api/v1 cuando .htaccess raíz esté validado (V1.1)
VITE_API_BASE_URL=/backend/public/api/v1
VITE_API_TIMEOUT_MS=30000

VITE_USE_MSW=false
VITE_PWA_ENABLED=false
